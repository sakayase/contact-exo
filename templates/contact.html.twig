<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire de contact</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script defer src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</head>
<body>  
    <div class="container">
        <h1>Formulaire de contact</h1>
        <form action="" method="post" novalidate>
            <div class="form-group">
                <label>Email address</label>
                <input type="email" class="{% if errors.email is not defined and data.email is not empty %}form-control is-valid{% elseif errors.email is defined%}form-control is-invalid{% else %}form-control{% endif %}" id="email" name="email" placeholder="name@example.com" {% if data.email is defined %} value={{data.email}} {% endif %}>
                {% if errors.email is defined %}
                    <div class="invalid-feedback">    
                        {{ errors.email }}
                    </div>
                {% elseif errors.email is not defined and data.email is not empty %}
                    <div class="valid-feedback">    
                        All is good    
                    </div>
                {% endif %}
            </div>
            <div class="form-group">
                <label>Object</label>
                <input type="text" class="{% if errors.object is not defined and data.object is not empty %}form-control is-valid{% elseif errors.object is defined%}form-control is-invalid{% else %}form-control{% endif %}" id="object" name="object" placeholder="Object" {% if data.object is defined %} value={{data.object}} {% endif %}>
                {% if errors.object is defined %}
                    <div class="invalid-feedback">    
                        {{ errors.object }}
                    </div>
                {% elseif errors.object is not defined and data.object is not empty %}
                    <div class="valid-feedback">    
                        All is good    
                    </div>
                {% endif %}
            </div>
            <div class="form-group">
                <label>Message</label>
                <textarea class="{% if errors.message is not defined and data.message is not empty %}form-control is-valid{% elseif errors.message is defined%}form-control is-invalid{% else %}form-control{% endif %}" id="message" name="message" rows="3">{% if data.message is defined %}{{data.message}}{% endif %}</textarea>
                {% if errors.message is defined %}
                    <div class="invalid-feedback">    
                        {{ errors.message }}    
                    </div>
                {% elseif errors.message is not defined and data.message is not empty %}
                    <div class="valid-feedback">    
                        All is good    
                    </div>
                {% endif %}
            </div>
            <button type="submit" class="btn btn-primary mb-2">Send</button>
        </form>
    </div>
</body>
</html>